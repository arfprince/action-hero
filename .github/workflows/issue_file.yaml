name: creat a comment on new issue

on:   
  issues:
    types: [opened]
jobs: 
  comment-with-action:
    runs-on: ubuntu-latest
    steps:
      - name: dumb-github-comment
        run: echo '${{ toJSON(github.event) }}' | jq
        shell: bash
